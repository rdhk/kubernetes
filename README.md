To build and push image to GCP Container Registry, run

    bash build.sh <version>


To create a new service stage, run

    bash create.sh <stage> <version>


To update existing service stage, run

    bash update.sh <stage> <version>
